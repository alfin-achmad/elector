/*
 * District Messages
 *
 * This contains all the text for the District container.
 */

import {defineMessages} from 'react-intl';

export const scope = 'app.containers.District';

export default defineMessages({
	header: {
		id: `${scope}.header`,
		defaultMessage: 'This is the District container!',
	},
});
